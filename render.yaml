services:
  - type: web
    name: hyperlocal-backend
    runtime: node
    rootDir: src/backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
